<%= form_with(model: daily_log) do |form| %>
  <% if daily_log.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(daily_log.errors.count, "error") %> prohibited this daily_log from being saved:</h2>

      <ul>
        <% daily_log.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
